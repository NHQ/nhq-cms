%li
  %span.credit= credit.title
  %br
  %span.name= credit.name
  - if user_signed_in?
    %span.admin= link_to(image_tag('trash.png', :alt => 'Destroy', :title => 'Delete Credit'), show_credit_path(@show, credit), :confirm => 'Are you sure you want to delete this Credit?', :method => :delete)
