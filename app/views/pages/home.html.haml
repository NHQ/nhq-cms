#eventsTab

#sidebar
  .upcoming
    - unless @upcoming_events.count < 1
      %ul.event_list
        - @upcoming_events.each do |event|
          %li.title
            - if event.venues.first
              = link_to "<span class='event_title'>#{event.title}</span><br /> @#{event.venues[0].venue_name}".html_safe, event
            - else
              = link_to "<span class='event_title'>#{event.title}</span>".html_safe, event


#main
  .promotions
    - @promotions.each do |promo|
      = render(:partial => 'promotions/promo', :locals => {:promotion => promo})


/ #feedTitle            
/   %a{:href=>"http://facebook.com/pages/500-Clown/73447773916", :target => "_blank"} Fanpage Updates