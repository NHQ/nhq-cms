<% title @page.title, !@page.hide_title %>

<div class="body"><%= @page.body.html_safe %></div>

<% if user_signed_in? -%>
	<p class="admin"><%= link_to('Edit', edit_page_path(@page)) + ' | ' + link_to('Back', pages_path) %></p>
<% end -%>
