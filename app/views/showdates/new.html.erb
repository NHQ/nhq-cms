<h1>Add date for <%= @event.title %></h1>

<% form_for @event.showdate, :class => "formtastic" do |f| %>
	<%= # f.text_field :start_date
	 %>
	<input id="datepicker" name="showdate[start_date]" size="30" type="text" />
  <%= submit_tag "Add Date(s)" %> or <%= link_to "cancel", :back %>
<% end %>