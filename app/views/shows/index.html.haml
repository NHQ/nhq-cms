%h1 Show listing

%table
  %tr
    %th
    %th
    %th
    %th

  - @shows.each do |show|
    %tr
      %td= link_to show.title, show
      %td.tags= 'Tags: ' + show.tags_array.inspect
      %td= link_to 'Edit', edit_show_path(show)
      %td= link_to 'Destroy', show, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New show', new_show_path
